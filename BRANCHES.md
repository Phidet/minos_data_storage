# MINOS SNTP branches

Every branch in the `NtpSt` tree of a MINOS SNTP file, grouped as the file
groups them.

The first column says whether the branch is in the archive by default:
**✓** exported, **✗** not. That is read from
[`branches.txt`](branches.txt), which is where the decision lives — see
[README.md](README.md) for how to change it. Anything whose meaning could
not be established is marked **???**.

Descriptions come from the MINOS LOON sources — the `NtpSR*`/`NtpMC*`/
`NtpTH*` class headers and the modules that fill them. Where a branch is
excluded, the description says why.

Two themes run through the exclusions, and are worth stating once:

- **Reconstruction is dropped as a matter of policy.** `trk`, `shw`, `slc`,
  `clu`, `evt` and everything keyed to them is what MINOS concluded from the
  data; the archive holds what the detector and the simulation recorded, so
  a future analysis can draw its own conclusions.
- **Redundancy is dropped only after it is checked.** Where a branch is
  excluded because it duplicates another, `check_exclusions.py` re-tests
  that claim on every file before conversion, and refuses the file if it
  fails. The derivations are in [`SCHEMA.md`](SCHEMA.md).

This file is generated by `make_branches_doc.py` from the manifest, so the
tick column cannot drift out of step with what the tool actually does.


## `header` — 4 of 19 archived

Run, subrun and snarl identifiers, plus ROOT and job bookkeeping. Only the four identifiers are archived.

| | Branch | Meaning |
|--|--------|---------|
| ✗ | `fUniqueID` | ROOT's `TObject` identifier. Internal to ROOT. |
| ✗ | `fBits` | ROOT's `TObject` status bits. Internal to ROOT. |
| ✗ | `fName` | ROOT object name. |
| ✗ | `fTitle` | ROOT object title. |
| ✗ | `fHeader.fVldContext.fDetector` | Which detector: Near, Far or CalDet. Constant within a file. |
| ✗ | `fHeader.fVldContext.fSimFlag` | Data, Monte Carlo, or a reroot/daq variant. Constant within a file. |
| ✗ | `fHeader.fVldContext.fTimeStamp.fSec` | Validity timestamp, whole seconds. Duplicated by the DAQ timing branches. |
| ✗ | `fHeader.fVldContext.fTimeStamp.fNanoSec` | The nanosecond part of the same timestamp. |
| ✓ | `fHeader.fRun` | DAQ run number. |
| ✓ | `fHeader.fSubRun` | DAQ subrun number. |
| ✗ | `fHeader.fRunType` | Run type: physics, calibration, test. Constant within a file. |
| ✗ | `fHeader.fErrorCode` | Error code for the record. |
| ✓ | `fHeader.fSnarl` | Snarl number within the run — the readout window this record describes. |
| ✗ | `fHeader.fTrigSrc` | Trigger source bitmask. Duplicated by `dataquality.trigsource`. |
| ✗ | `fHeader.fTimeFrame` | Time frame number. Duplicated by `timestatus.timeframe`. |
| ✗ | `fHeader.fRemoteSpillType` | Spill type as sent from the accelerator. Duplicated by `dataquality.spilltype`. |
| ✓ | `fHeader.fEvent` | Event number within the snarl. Constant `-1` in every file checked — the reconstruction chain assigns it. |
| ✗ | `fIsClearable` | Whether the record may be cleared from memory. A framework flag. |
| ✗ | `fJobHistory.fJobRecordMap` | Which modules processed the record. Job provenance, not event data. |

## `evthdr` — 0 of 38 archived

Per-snarl counts of what the reconstruction found, plus the snarl's summed pulse height and date. All of it is either a count of reconstructed objects or recoverable from the branches kept.

| | Branch | Meaning |
|--|--------|---------|
| ✗ | `evthdr.ndigit` | Digits in the snarl. |
| ✗ | `evthdr.nstrip` | Strips hit in the snarl. Recoverable by counting `stp` rows. |
| ✗ | `evthdr.nslice` | Slices the reconstruction found. |
| ✗ | `evthdr.ncluster` | Clusters it found. |
| ✗ | `evthdr.ntrack` | Tracks it found. |
| ✗ | `evthdr.nshower` | Showers it found. |
| ✗ | `evthdr.nevent` | Reconstructed events it built. |
| ✗ | `evthdr.trigtime` | Trigger time for the snarl. Duplicated by `dataquality.trigtime`. |
| ✗ | `evthdr.litime` | Light-injection pulse time. Duplicated by `dataquality.litime`. |
| ✗ | `evthdr.ph.raw` | Summed raw ADC over the whole snarl. |
| ✗ | `evthdr.ph.siglin` | The same, linearity-corrected. |
| ✗ | `evthdr.ph.sigcor` | The same, also corrected for fibre attenuation. |
| ✗ | `evthdr.ph.pe` | The same, in photoelectrons. |
| ✗ | `evthdr.planeall.n` | Planes in the snarl with at least one digit. |
| ✗ | `evthdr.planeall.nu` | Of those, planes in the u view. |
| ✗ | `evthdr.planeall.nv` | Of those, planes in the v view. |
| ✗ | `evthdr.planeall.beg` | Lowest plane carrying a digit. |
| ✗ | `evthdr.planeall.begu` | The same in the u view. |
| ✗ | `evthdr.planeall.begv` | The same in the v view. |
| ✗ | `evthdr.planeall.end` | Highest plane carrying a digit. |
| ✗ | `evthdr.planeall.endu` | The same in the u view. |
| ✗ | `evthdr.planeall.endv` | The same in the v view. |
| ✗ | `evthdr.plane.n` | Planes in the snarl's active range with a non-zero readout. The range is found by looking for four contiguous planes whose summed pulse height across both ends exceeds 3 pe; it runs from the first such group to the last, so isolated noise hits outside it do not count. |
| ✗ | `evthdr.plane.nu` | Of those, planes in the u view. |
| ✗ | `evthdr.plane.nv` | Of those, planes in the v view. |
| ✗ | `evthdr.plane.beg` | First plane of that range. |
| ✗ | `evthdr.plane.begu` | First u-view plane of it. |
| ✗ | `evthdr.plane.begv` | First v-view plane of it. |
| ✗ | `evthdr.plane.end` | Last plane of that range. |
| ✗ | `evthdr.plane.endu` | Last u-view plane of it. |
| ✗ | `evthdr.plane.endv` | Last v-view plane of it. |
| ✗ | `evthdr.date.year` | Calendar year of the snarl. |
| ✗ | `evthdr.date.month` | Month. |
| ✗ | `evthdr.date.day` | Day. |
| ✗ | `evthdr.date.hour` | Hour. |
| ✗ | `evthdr.date.minute` | Minute. |
| ✗ | `evthdr.date.sec` | Second. |
| ✗ | `evthdr.date.utc` | Whether the above are UTC. |

## `vetohdr` — 0 of 12 archived

Veto shield summary for a reconstructed track: what the shield saw around the time the track crossed it. Indexed `[3]` by time window relative to the track — `0` pre-trigger, `1` in time, `2` post-trigger. All of it is keyed to a reconstructed track, and the shield's actual hits are kept in `vetostp`.

| | Branch | Meaning |
|--|--------|---------|
| ✗ | `vetohdr.ndigit[3]` | Shield digits in each time window. |
| ✗ | `vetohdr.nplank[3]` | Shield planks hit in each time window. |
| ✗ | `vetohdr.adc[3]` | Summed shield ADC in each time window. |
| ✗ | `vetohdr.dx[3]` | Closest approach between the track projection and a shield hit, per window [m]. |
| ✗ | `vetohdr.dxvetostp[3]` | Index into `vetostp` of the hit that achieved it. |
| ✗ | `vetohdr.dcos` | Direction cosine of the track where it was projected to the shield. |
| ✗ | `vetohdr.projx` | x where the track was projected to cross the shield [m]. |
| ✗ | `vetohdr.projy` | y of the same projection [m]. |
| ✗ | `vetohdr.projz` | z of the same projection [m]. |
| ✗ | `vetohdr.exphits` | Shield crossings expected for this track. |
| ✗ | `vetohdr.ishit` | Whether the shield was hit at all. |
| ✗ | `vetohdr.found2sect` | Whether hits were found in two shield sections. |

## `crhdr` — 0 of 7 archived

Cosmic-ray direction for the snarl, derived from a reconstructed track. Meaningless without the track it came from.

| | Branch | Meaning |
|--|--------|---------|
| ✗ | `crhdr.zenith` | Zenith angle of the track [rad]. |
| ✗ | `crhdr.azimuth` | Azimuth of the track [rad]. |
| ✗ | `crhdr.ra` | Right ascension of the arrival direction [rad]. |
| ✗ | `crhdr.rahourangle` | The same expressed as an hour angle. |
| ✗ | `crhdr.dec` | Declination of the arrival direction [rad]. |
| ✗ | `crhdr.juliandate` | Julian date of the snarl. |
| ✗ | `crhdr.locsiderialtime` | Local sidereal time at the detector. |

## `dmxstatus` — 0 of 9 archived

Quality of demultiplexing — the step that resolves which of the several strips sharing a Near Detector readout channel actually fired. A reconstruction step, and its failure flags describe that step.

| | Branch | Meaning |
|--|--------|---------|
| ✗ | `dmxstatus.ismultimuon` | Whether the snarl was tagged as multi-muon. |
| ✗ | `dmxstatus.nonphysicalfail` | Demultiplexing produced a non-physical result. |
| ✗ | `dmxstatus.validplanesfail` | Too few valid planes to demultiplex. |
| ✗ | `dmxstatus.vertexplanefail` | The vertex plane could not be resolved. |
| ✗ | `dmxstatus.ustrayplanes` | U-view planes whose hits could not be assigned. |
| ✗ | `dmxstatus.vstrayplanes` | The same in the v view. |
| ✗ | `dmxstatus.uvalidplanes` | U-view planes successfully demultiplexed. |
| ✗ | `dmxstatus.vvalidplanes` | The same in the v view. |
| ✗ | `dmxstatus.avgtimeoffset` | Mean timing offset applied during demultiplexing [s]. |

## `detstatus` — 7 of 7 archived

Magnet and high-voltage state. Needed to interpret momentum and charge sign, so all of it is archived.

| | Branch | Meaning |
|--|--------|---------|
| ✓ | `detstatus.coilstatus` | Magnet coil on/off and polarity. |
| ✓ | `detstatus.dcscoilstatus` | The same as reported by the slow-control system. |
| ✓ | `detstatus.coilcurrent1` | Magnet coil current, which sets the field and hence the momentum and charge-sign measurement [A]. |
| ✓ | `detstatus.coilcurrent2` | Second coil current reading [A]. |
| ✓ | `detstatus.dbuhvstatus` | Photomultiplier high-voltage status. |
| ✓ | `detstatus.coldchips1` | Front-end chips below high-voltage threshold in supermodule 1. |
| ✓ | `detstatus.coldchips2` | The same for supermodule 2. |

## `timestatus` — 6 of 6 archived

Absolute timing for the snarl. Archived: without it a snarl cannot be placed on the beam clock.

| | Branch | Meaning |
|--|--------|---------|
| ✓ | `timestatus.sgate_10mhz` | Spill gate counted on the 10 MHz clock. |
| ✓ | `timestatus.sgate_53mhz` | Spill gate counted on the 53 MHz clock. |
| ✓ | `timestatus.rollover_53mhz` | How many times the 53 MHz counter has wrapped. |
| ✓ | `timestatus.rollover_last_53mhz` | The rollover count at the previous snarl, so an interval spanning a wrap can be unwrapped. |
| ✓ | `timestatus.crate_t0_ns` | Crate time zero [ns]. |
| ✓ | `timestatus.timeframe` | Time frame number. |

## `calstatus` — 1 of 1 archived

Calibration constants in force for the snarl.

| | Branch | Meaning |
|--|--------|---------|
| ✓ | `calstatus.gevpermip` | Constant converting MIP-equivalent signal to GeV. Needed to turn the `pe` values into energy. |

## `dataquality` — 25 of 25 archived

Beam spill, trigger and DAQ state for the snarl, including the light-injection pulser. Unset (`-1`) for Monte Carlo. All of it is archived: it describes the conditions the data was taken under, and nothing else records them.

| | Branch | Meaning |
|--|--------|---------|
| ✓ | `dataquality.trigsource` | What triggered the readout, as a bitmask. |
| ✓ | `dataquality.trigtime` | Trigger time. |
| ✓ | `dataquality.errorcode` | DAQ error code for the snarl. |
| ✓ | `dataquality.cratemask` | How many readout crates were active; 16 is a full Far Detector readout. |
| ✓ | `dataquality.pretrigdigits` | Digits recorded before the trigger. |
| ✓ | `dataquality.posttrigdigits` | Digits recorded after it. |
| ✓ | `dataquality.snarlmultiplicity` | Interactions in this snarl. |
| ✓ | `dataquality.spillstatus` | Beam spill status. |
| ✓ | `dataquality.spilltype` | Beam spill type. |
| ✓ | `dataquality.spilltimeerror` | Error on the spill timing. |
| ✓ | `dataquality.litrigger` | Whether this snarl was a light-injection trigger. |
| ✓ | `dataquality.litime` | Light-injection pulse time. |
| ✓ | `dataquality.lisubtractedtime` | The same with the pedestal offset removed. |
| ✓ | `dataquality.lirelativetime` | Time relative to the light-injection trigger. |
| ✓ | `dataquality.licalibpoint` | Which point in the light-injection calibration sequence. |
| ✓ | `dataquality.licalibtype` | Which calibration type was running. |
| ✓ | `dataquality.libox` | Which pulser box fired. |
| ✓ | `dataquality.liled` | Which LED within that box. |
| ✓ | `dataquality.lipulseheight` | Pulser amplitude setting. |
| ✓ | `dataquality.lipulsewidth` | Pulser width setting. |
| ✓ | `dataquality.coldchips` | Front-end chips reading nothing. |
| ✓ | `dataquality.hotchips` | Chips firing far above their expected rate. |
| ✓ | `dataquality.busychips` | Chips saturated by readout load. |
| ✓ | `dataquality.readouterrors` | Readout errors in the snarl. |
| ✓ | `dataquality.dataqualityword` | Packed overall data-quality flag for the snarl. |

## `mchdr` — 0 of 8 archived

Which generator produced the file, and on what machine. Job provenance rather than event data, and constant within a file.

| | Branch | Meaning |
|--|--------|---------|
| ✗ | `mchdr.error` | Generator error code. |
| ✗ | `mchdr.nmc` | Interaction records in the file. |
| ✗ | `mchdr.nstdhep` | Truth particle records in the file. |
| ✗ | `mchdr.ndigihit` | Per-digit truth records in the file. |
| ✗ | `mchdr.geninfo.time.fSec` | When the generator ran, whole seconds. |
| ✗ | `mchdr.geninfo.time.fNanoSec` | The nanosecond part. |
| ✗ | `mchdr.geninfo.codename` | Generator codename. |
| ✗ | `mchdr.geninfo.hostname` | Machine it ran on. |

## `photon` — 0 of 13 archived

Counters from the optical simulation: how much light was made and how much was thrown away. Describes how the simulation ran, not what physically happened — the resulting light is already in `stp.ph*`. Simulating every photon is too slow, so a fraction is tracked and scaled up; both the scaled and unscaled counts are here.

| | Branch | Meaning |
|--|--------|---------|
| ✗ | `photon.hitsDiscardedGeom` | Hits dropped because they fell outside the simulated geometry. |
| ✗ | `photon.hitsDiscardedBad` | Hits dropped as unusable. |
| ✗ | `photon.totalHits` | Scintillator hits simulated. |
| ✗ | `photon.totalStripsHit` | Distinct strips those hits touched. |
| ✗ | `photon.bluePhotons` | Blue scintillation photons produced, prescale-corrected. |
| ✗ | `photon.greenPhotons` | Green photons re-emitted by the wavelength-shifting fibre, prescale-corrected. |
| ✗ | `photon.bluePhotons_nonprescaled` | Blue photons actually tracked, before the prescale correction. |
| ✗ | `photon.greenPhotons_nonprescaled` | The same for green photons. |
| ✗ | `photon.totalPE` | Photoelectrons produced. |
| ✗ | `photon.totalPixels` | Photomultiplier pixels they landed on. |
| ✗ | `photon.totalHitEnergy` | Energy deposited by the simulated hits [GeV]. |
| ✗ | `photon.energyDiscardedGeom` | Energy in the hits dropped on geometry. |
| ✗ | `photon.energyDiscardedBad` | Energy in the hits dropped as unusable. |

## `detsim` — 0 of 17 archived

Counters from the electronics simulation: how many digits survived each stage of the front-end and DAQ. A record of the simulation's behaviour rather than the event's.

| | Branch | Meaning |
|--|--------|---------|
| ✗ | `detsim.timeShift` | Time offset applied to the simulated snarl [s]. |
| ✗ | `detsim.nPE` | Photoelectrons entering the electronics simulation. |
| ✗ | `detsim.hitPixels` | Photomultiplier pixels hit. |
| ✗ | `detsim.hitPixelsWithXtalk` | The same including cross-talk between pixels. |
| ✗ | `detsim.digitsAfterFETrigger` | Digits surviving the front-end trigger. |
| ✗ | `detsim.digitsAfterSpars` | Digits surviving sparsification. |
| ✗ | `detsim.digitsAfterDaqTrigger` | Digits surviving the DAQ trigger. |
| ✗ | `detsim.totalPE` | Summed photoelectrons. |
| ✗ | `detsim.totalCharge` | Summed charge [ADC]. |
| ✗ | `detsim.adcsAfterFETrigger` | Summed ADC surviving the front-end trigger. |
| ✗ | `detsim.adcsAfterSpars` | Summed ADC surviving sparsification. |
| ✗ | `detsim.adcsAfterDaqTrigger` | Summed ADC surviving the DAQ trigger. |
| ✗ | `detsim.bigSnarl` | Whether the simulated snarl was flagged as unusually large. |
| ✗ | `detsim.snarls` | Snarls the simulation split this event into. |
| ✗ | `detsim.snarlDigits` | Digits in each of those snarls. |
| ✗ | `detsim.snarlTrigger` | Trigger decision for each of them. |
| ✗ | `detsim.snarlAdcs` | Summed ADC in each of them. |

## `vetostp` — 13 of 14 archived

Raw hits in the veto shield — the scintillator blanket over the Far Detector used to tag entering cosmic-ray muons. A measurement, so it is archived; the summary keyed to reconstructed tracks (`vetohdr`, `vetoexp`) is not. Fields indexed `[2]` are one value per strip end.

| | Branch | Meaning |
|--|--------|---------|
| ✗ | `vetostp.index` | Position of the hit in its array. Row order already carries it. |
| ✓ | `vetostp.ndigit` | Digits on this shield strip. |
| ✓ | `vetostp.pln` | Shield plane. |
| ✓ | `vetostp.plank` | Shield plank within the plane. |
| ✓ | `vetostp.x` | Position [m]. |
| ✓ | `vetostp.y` | Position [m]. |
| ✓ | `vetostp.z[2]` | Position at each strip end [m]. |
| ✓ | `vetostp.adc[2]` | Raw pulse height at each end [ADC]. |
| ✓ | `vetostp.pmtindex[2]` | Which photomultiplier channel each end is read out by. |
| ✓ | `vetostp.pmtpixel[2]` | Which pixel on that photomultiplier. |
| ✓ | `vetostp.wlspigtail[2]` | Length of wavelength-shifting fibre from the strip end [m]. |
| ✓ | `vetostp.clearlen[2]` | Length of clear fibre from there to the photomultiplier [m]. |
| ✓ | `vetostp.time[2]` | Calibrated hit time at each end [s]. |
| ✓ | `vetostp.timeraw[2]` | The same before timing calibration [s]. |

## `vetoexp` — 0 of 10 archived

Where a *reconstructed track* was expected to cross the shield — a projection, not a measurement. Meaningless without the track, and the shield's actual hits are kept in `vetostp`.

| | Branch | Meaning |
|--|--------|---------|
| ✗ | `vetoexp.plane` | Shield plane the crossing was expected in. |
| ✗ | `vetoexp.plank` | Shield plank it was expected in. |
| ✗ | `vetoexp.stripinplank[2]` | Which strips within that plank, one per end. |
| ✗ | `vetoexp.projx` | x of the expected crossing [m]. |
| ✗ | `vetoexp.projy` | y of the expected crossing [m]. |
| ✗ | `vetoexp.projz` | z of the expected crossing [m]. |
| ✗ | `vetoexp.centerdis` | Distance from the plank centre to the expected crossing [m]. |
| ✗ | `vetoexp.isfound` | Whether a shield hit was actually found there. |
| ✗ | `vetoexp.stripdigit` | Index of the digit found, if any. |
| ✗ | `vetoexp.index` | Position of the record in its array. |

## `deadchips` — 0 of 6 archived

Map of dead electronics channels. Genuinely useful for efficiency work, but **empty in every file checked**, so there is nothing to archive. `check_exclusions.py` refuses any file where that stops being true.

| | Branch | Meaning |
|--|--------|---------|
| ✗ | `deadchips.channelid` | Identifier of the dead channel. |
| ✗ | `deadchips.plane0` | First plane the channel serves. |
| ✗ | `deadchips.plane1` | Last plane it serves. |
| ✗ | `deadchips.shield` | Whether the channel belongs to the veto shield. |
| ✗ | `deadchips.errorcode` | Why the channel was marked dead. |
| ✗ | `deadchips.status` | Status flag for the channel. |

## `stp` — 11 of 20 archived

Digitised strip hits: the detector's actual image of the event, and the core of the archive. One row per strip that fired.

| | Branch | Meaning |
|--|--------|---------|
| ✗ | `stp.index` | Position of the strip in its array. Row order already carries it. |
| ✓ | `stp.planeview` | Which stereo view the strip belongs to: `2` = U, `3` = V. Fixed by `plane`. |
| ✗ | `stp.ndigit` | How many ends of the strip fired, only ever 1 or 2. The same information as which of `time0`/`time1` holds the `-999999` sentinel. |
| ✗ | `stp.demuxveto` | Output of demultiplexing, which resolves the Near Detector's several-strips-per-channel readout. Constant `0` in the Far Detector files here, and a reconstruction step in any case. |
| ✓ | `stp.strip` | Strip number within the plane, 0–191. |
| ✓ | `stp.plane` | Plane number along the beam axis, 1–485 in a Far Detector file. |
| ✗ | `stp.tpos` | Transverse position of the strip [m]. Fixed by `plane` and `strip` together. |
| ✗ | `stp.z` | Position along the beam axis [m]. One fixed z per plane, so a lookup on `plane`. |
| ✗ | `stp.pmtindex0` | Which photomultiplier channel the east end is wired to. Fixed per strip, and a property of the readout map rather than the event. |
| ✗ | `stp.pmtindex1` | The same for the west end. |
| ✓ | `stp.time0` | Charge-weighted mean hit time, east end [s], relative to the trigger. `-999999` means that end saw no signal. |
| ✓ | `stp.time1` | The same, west end. |
| ✓ | `stp.ph0.raw` | Raw ADC, east end — what the electronics recorded, before any correction. |
| ✗ | `stp.ph0.siglin` | The same, linearity-corrected but not yet attenuation-corrected. Both endpoints of that chain are kept and the factor between them is recoverable from the pair, so the middle step adds little. |
| ✓ | `stp.ph0.sigcor` | Attenuation-normalised strip response, east end. |
| ✓ | `stp.ph0.pe` | Calibrated light yield, east end [photoelectrons]. |
| ✓ | `stp.ph1.raw` | Raw ADC, west end. |
| ✗ | `stp.ph1.siglin` | The intermediate stage, west end. |
| ✓ | `stp.ph1.sigcor` | Attenuation-normalised strip response, west end. |
| ✓ | `stp.ph1.pe` | Calibrated light yield, west end [photoelectrons]. |

## `slc` — 0 of 18 archived

Reconstructed slices — the first stage of reconstruction, grouping a snarl's hits into what looks like separate interactions.

| | Branch | Meaning |
|--|--------|---------|
| ✗ | `slc.index` | Position of the slice in its array. Row order already carries it. |
| ✗ | `slc.ndigit` | Digits (single-end readouts) making up the slice. |
| ✗ | `slc.nstpcnt` | Strips counted toward the slice, including repeats across planes. |
| ✗ | `slc.nstrip` | Strips making up the slice. |
| ✗ | `slc.stp` | Indices of the strips in this slice, into the `stp` array. |
| ✗ | `slc.ph.raw` | Summed raw ADC over the slice. |
| ✗ | `slc.ph.siglin` | The same, linearity-corrected. |
| ✗ | `slc.ph.sigcor` | The same, also corrected for fibre attenuation. |
| ✗ | `slc.ph.pe` | The same, in photoelectrons. |
| ✗ | `slc.plane.n` | Planes hit by the slice. |
| ✗ | `slc.plane.nu` | Of those, planes in the u view. |
| ✗ | `slc.plane.nv` | Of those, planes in the v view. |
| ✗ | `slc.plane.beg` | First plane of the slice. |
| ✗ | `slc.plane.begu` | First u-view plane of the slice. |
| ✗ | `slc.plane.begv` | First v-view plane of the slice. |
| ✗ | `slc.plane.end` | Last plane of the slice. |
| ✗ | `slc.plane.endu` | Last u-view plane of the slice. |
| ✗ | `slc.plane.endv` | Last v-view plane of the slice. |

## `clu` — 0 of 23 archived

Clusters — groups of hits within one view, upstream of track and shower fitting.

| | Branch | Meaning |
|--|--------|---------|
| ✗ | `clu.index` | Position of the cluster in its array. Row order already carries it. |
| ✗ | `clu.planeview` | Which view the cluster lies in: `2` = U, `3` = V. |
| ✗ | `clu.nplane` | Planes the cluster spans. |
| ✗ | `clu.begplane` | First plane of the cluster. |
| ✗ | `clu.endplane` | Last plane of the cluster. |
| ✗ | `clu.id` | Cluster type identifier assigned by the clustering algorithm. |
| ✗ | `clu.slc` | Index of the slice this cluster belongs to. |
| ✗ | `clu.ndigit` | Digits (single-end readouts) making up the cluster. |
| ✗ | `clu.nstpcnt` | Strips counted toward the cluster, including repeats across planes. |
| ✗ | `clu.nstrip` | Strips making up the cluster. |
| ✗ | `clu.stp` | Indices of the strips in this cluster, into the `stp` array. |
| ✗ | `clu.probem` | Likelihood the cluster is electromagnetic rather than hadronic. |
| ✗ | `clu.zvtx` | z of the cluster's start [m]. |
| ✗ | `clu.tposvtx` | Transverse position of the cluster's start [m]. |
| ✗ | `clu.slope` | Slope of the cluster in the transverse-versus-z plane. |
| ✗ | `clu.avgdev` | Mean deviation of the hits from that slope — how straight the cluster is. |
| ✗ | `clu.ph.raw` | Summed raw ADC over the cluster. |
| ✗ | `clu.ph.siglin` | The same, linearity-corrected. |
| ✗ | `clu.ph.sigcor` | The same, also corrected for fibre attenuation. |
| ✗ | `clu.ph.pe` | The same, in photoelectrons. |
| ✗ | `clu.ph.sigmap` | The same, normalised by the strip-to-strip response map. |
| ✗ | `clu.ph.mip` | The same, in units of a minimum-ionising particle. |
| ✗ | `clu.ph.gev` | The same, converted to GeV. |

## `shw` — 0 of 80 archived

Reconstructed showers — hadronic or electromagnetic energy deposits, fitted from the clusters.

| | Branch | Meaning |
|--|--------|---------|
| ✗ | `shw.index` | Position of the shower in its array. Row order already carries it. |
| ✗ | `shw.slc` | Index of the slice this shower belongs to. |
| ✗ | `shw.ndigit` | Digits (single-end readouts) making up the shower. |
| ✗ | `shw.nstpcnt` | Strips counted toward the shower, including repeats across planes. |
| ✗ | `shw.nstrip` | Strips making up the shower. |
| ✗ | `shw.stp` | Indices of the strips belonging to this shower, into the `stp` array. |
| ✗ | `shw.stpu` | U position of the shower at each strip's plane [m]. |
| ✗ | `shw.stpv` | V position of the shower at each strip's plane [m]. |
| ✗ | `shw.stpx` | x position of the shower at each strip's plane [m]. |
| ✗ | `shw.stpy` | y position of the shower at each strip's plane [m]. |
| ✗ | `shw.stpz` | z position of the shower at each strip's plane [m]. |
| ✗ | `shw.stpph0sigmap` | Attenuation-corrected pulse height at each strip, east end. |
| ✗ | `shw.stpph0mip` | The same in MIPs, east end. |
| ✗ | `shw.stpph0gev` | The same in GeV, east end. |
| ✗ | `shw.stpph1sigmap` | Attenuation-corrected pulse height at each strip, west end. |
| ✗ | `shw.stpph1mip` | The same in MIPs, west end. |
| ✗ | `shw.stpph1gev` | The same in GeV, west end. |
| ✗ | `shw.stpattn0c0` | Attenuation constant C0 from the fibre mapper, east end. |
| ✗ | `shw.stpattn1c0` | The same, west end. |
| ✗ | `shw.stpt0` | Hit time at each strip corrected for propagation delay, east end [s]. |
| ✗ | `shw.stpt1` | The same, west end [s]. |
| ✗ | `shw.stptcal0t0` | Calibration T0 offset applied at each strip, east end [s]. |
| ✗ | `shw.stptcal1t0` | The same, west end [s]. |
| ✗ | `shw.nUcluster` | Clusters in the u view making up the shower. |
| ✗ | `shw.nVcluster` | The same in the v view. |
| ✗ | `shw.ncluster` | Clusters in total. |
| ✗ | `shw.clu` | Indices of those clusters, into the `clu` array. |
| ✗ | `shw.contained` | Whether the shower is fully contained in the detector. |
| ✗ | `shw.ph.raw` | Summed raw ADC over the shower. |
| ✗ | `shw.ph.siglin` | The same, linearity-corrected. |
| ✗ | `shw.ph.sigcor` | The same, also corrected for fibre attenuation. |
| ✗ | `shw.ph.pe` | The same, in photoelectrons. |
| ✗ | `shw.ph.sigmap` | The same, normalised by the strip-to-strip response map. |
| ✗ | `shw.ph.mip` | The same, in units of a minimum-ionising particle. |
| ✗ | `shw.ph.gev` | The same, converted to GeV. |
| ✗ | `shw.shwph.linCCgev` | Shower energy under the charged-current hypothesis, linear calibration [GeV]. |
| ✗ | `shw.shwph.wtCCgev` | The same with the weighted calibration [GeV]. |
| ✗ | `shw.shwph.linNCgev` | Shower energy under the neutral-current hypothesis, linear calibration [GeV]. |
| ✗ | `shw.shwph.wtNCgev` | The same with the weighted calibration [GeV]. |
| ✗ | `shw.shwph.EMgev` | Shower energy under an electromagnetic hypothesis [GeV]. |
| ✗ | `shw.plane.n` | Planes hit by the shower. |
| ✗ | `shw.plane.nu` | Of those, planes in the u view. |
| ✗ | `shw.plane.nv` | Of those, planes in the v view. |
| ✗ | `shw.plane.beg` | First plane of the shower. |
| ✗ | `shw.plane.begu` | First u-view plane of the shower. |
| ✗ | `shw.plane.begv` | First v-view plane of the shower. |
| ✗ | `shw.plane.end` | Last plane of the shower. |
| ✗ | `shw.plane.endu` | Last u-view plane of the shower. |
| ✗ | `shw.plane.endv` | Last v-view plane of the shower. |
| ✗ | `shw.vtx.plane` | Plane number at the shower start. |
| ✗ | `shw.vtx.u` | U coordinate at the shower start [m]. |
| ✗ | `shw.vtx.v` | V coordinate at the shower start [m]. |
| ✗ | `shw.vtx.x` | x at the shower start [m]. |
| ✗ | `shw.vtx.y` | y at the shower start [m]. |
| ✗ | `shw.vtx.z` | z at the shower start [m], along the beam axis. |
| ✗ | `shw.vtx.t` | Time at the shower start [s]. |
| ✗ | `shw.vtx.dcosu` | Direction cosine along u at the shower start. |
| ✗ | `shw.vtx.dcosv` | Direction cosine along v at the shower start. |
| ✗ | `shw.vtx.dcosx` | Direction cosine along x at the shower start. |
| ✗ | `shw.vtx.dcosy` | Direction cosine along y at the shower start. |
| ✗ | `shw.vtx.dcosz` | Direction cosine along z at the shower start. |
| ✗ | `shw.vtx.eu` | Uncertainty on u at the shower start. |
| ✗ | `shw.vtx.ev` | Uncertainty on v at the shower start. |
| ✗ | `shw.vtx.ex` | Uncertainty on x at the shower start. |
| ✗ | `shw.vtx.ey` | Uncertainty on y at the shower start. |
| ✗ | `shw.vtx.edcosu` | Uncertainty on the u direction cosine at the shower start. |
| ✗ | `shw.vtx.edcosv` | Uncertainty on the v direction cosine at the shower start. |
| ✗ | `shw.vtx.edcosx` | Uncertainty on the x direction cosine at the shower start. |
| ✗ | `shw.vtx.edcosy` | Uncertainty on the y direction cosine at the shower start. |
| ✗ | `shw.vtx.edcosz` | Uncertainty on the z direction cosine at the shower start. |
| ✗ | `shw.sss.nTrkLikeU` | Track-like sub-showers found in the u view. |
| ✗ | `shw.sss.nTrkLikeV` | The same in the v view. |
| ✗ | `shw.sss.nRecoTrkU` | Reconstructed tracks overlapping the shower in the u view. |
| ✗ | `shw.sss.nRecoTrkV` | The same in the v view. |
| ✗ | `shw.sss.phTrkLikeU` | Pulse height in the track-like part, u view. |
| ✗ | `shw.sss.phTrkLikeV` | The same in the v view. |
| ✗ | `shw.sss.probEMU` | Likelihood the u-view shower is electromagnetic. |
| ✗ | `shw.sss.probEMV` | The same in the v view. |
| ✗ | `shw.sss.compactU` | How compact the shower is in the u view. |
| ✗ | `shw.sss.compactV` | The same in the v view. |

## `trk` — 0 of 162 archived

Reconstructed tracks — fitted muon trajectories, and the largest group in the file. All of it is reconstruction output.

| | Branch | Meaning |
|--|--------|---------|
| ✗ | `trk.index` | Position of the track in its array. Row order already carries it. |
| ✗ | `trk.slc` | Index of the slice this track belongs to. |
| ✗ | `trk.ndigit` | Digits (single-end readouts) making up the track. |
| ✗ | `trk.nstpcnt` | Strips counted toward the track, including repeats across planes. |
| ✗ | `trk.nstrip` | Strips making up the track. |
| ✗ | `trk.stp` | Indices of the strips belonging to this track, into the `stp` array. |
| ✗ | `trk.stpfit` | Whether each strip was used in the track fit. |
| ✗ | `trk.stpu` | U position of the track at each strip's plane [m]. |
| ✗ | `trk.stpv` | V position of the track at each strip's plane [m]. |
| ✗ | `trk.stpx` | x position of the track at each strip's plane [m]. |
| ✗ | `trk.stpy` | y position of the track at each strip's plane [m]. |
| ✗ | `trk.stpz` | z position of the track at each strip's plane [m]. |
| ✗ | `trk.stpds` | Path length from the track end to each strip [m]. |
| ✗ | `trk.stpfitchi2` | Contribution of each strip to the fit χ². |
| ✗ | `trk.stpfitprechi2` | The same before the final iteration. |
| ✗ | `trk.stpfitqp` | Fitted charge-over-momentum at each strip [e/GeV]. |
| ✗ | `trk.stpph0sigmap` | Attenuation-corrected pulse height at each strip, east end. |
| ✗ | `trk.stpph0mip` | The same in MIPs, east end. |
| ✗ | `trk.stpph0gev` | The same in GeV, east end. |
| ✗ | `trk.stpph1sigmap` | Attenuation-corrected pulse height at each strip, west end. |
| ✗ | `trk.stpph1mip` | The same in MIPs, west end. |
| ✗ | `trk.stpph1gev` | The same in GeV, west end. |
| ✗ | `trk.stpattn0c0` | Attenuation constant C0 from the fibre mapper, east end. |
| ✗ | `trk.stpattn1c0` | The same, west end. |
| ✗ | `trk.stpt0` | Hit time at each strip corrected for propagation delay, east end [s]. |
| ✗ | `trk.stpt1` | The same, west end [s]. |
| ✗ | `trk.stptcal0t0` | Calibration T0 offset applied at each strip, east end [s]. |
| ✗ | `trk.stptcal1t0` | The same, west end [s]. |
| ✗ | `trk.ds` | Track path length [m]. |
| ✗ | `trk.range` | Range of the track through the steel [g/cm²]. |
| ✗ | `trk.cputime` | CPU seconds spent reconstructing the track. |
| ✗ | `trk.contained` | Whether the track is fully contained in the detector. |
| ✗ | `trk.ph.raw` | Summed raw ADC over the track. |
| ✗ | `trk.ph.siglin` | The same, linearity-corrected. |
| ✗ | `trk.ph.sigcor` | The same, also corrected for fibre attenuation. |
| ✗ | `trk.ph.pe` | The same, in photoelectrons. |
| ✗ | `trk.ph.sigmap` | The same, normalised by the strip-to-strip response map. |
| ✗ | `trk.ph.mip` | The same, in units of a minimum-ionising particle. |
| ✗ | `trk.ph.gev` | The same, converted to GeV. |
| ✗ | `trk.plane.n` | Planes hit by the track. |
| ✗ | `trk.plane.nu` | Of those, planes in the u view. |
| ✗ | `trk.plane.nv` | Of those, planes in the v view. |
| ✗ | `trk.plane.beg` | First plane of the track. |
| ✗ | `trk.plane.begu` | First u-view plane of the track. |
| ✗ | `trk.plane.begv` | First v-view plane of the track. |
| ✗ | `trk.plane.end` | Last plane of the track. |
| ✗ | `trk.plane.endu` | Last u-view plane of the track. |
| ✗ | `trk.plane.endv` | Last v-view plane of the track. |
| ✗ | `trk.plane.ntrklike` | Planes carrying a track-like hit pattern. |
| ✗ | `trk.vtx.plane` | Plane number at the track start. |
| ✗ | `trk.vtx.u` | U coordinate at the track start [m]. |
| ✗ | `trk.vtx.v` | V coordinate at the track start [m]. |
| ✗ | `trk.vtx.x` | x at the track start [m]. |
| ✗ | `trk.vtx.y` | y at the track start [m]. |
| ✗ | `trk.vtx.z` | z at the track start [m], along the beam axis. |
| ✗ | `trk.vtx.t` | Time at the track start [s]. |
| ✗ | `trk.vtx.dcosu` | Direction cosine along u at the track start. |
| ✗ | `trk.vtx.dcosv` | Direction cosine along v at the track start. |
| ✗ | `trk.vtx.dcosx` | Direction cosine along x at the track start. |
| ✗ | `trk.vtx.dcosy` | Direction cosine along y at the track start. |
| ✗ | `trk.vtx.dcosz` | Direction cosine along z at the track start. |
| ✗ | `trk.vtx.eu` | Uncertainty on u at the track start. |
| ✗ | `trk.vtx.ev` | Uncertainty on v at the track start. |
| ✗ | `trk.vtx.ex` | Uncertainty on x at the track start. |
| ✗ | `trk.vtx.ey` | Uncertainty on y at the track start. |
| ✗ | `trk.vtx.edcosu` | Uncertainty on the u direction cosine at the track start. |
| ✗ | `trk.vtx.edcosv` | Uncertainty on the v direction cosine at the track start. |
| ✗ | `trk.vtx.edcosx` | Uncertainty on the x direction cosine at the track start. |
| ✗ | `trk.vtx.edcosy` | Uncertainty on the y direction cosine at the track start. |
| ✗ | `trk.vtx.edcosz` | Uncertainty on the z direction cosine at the track start. |
| ✗ | `trk.end.plane` | Plane number at the track end. |
| ✗ | `trk.end.u` | U coordinate at the track end [m]. |
| ✗ | `trk.end.v` | V coordinate at the track end [m]. |
| ✗ | `trk.end.x` | x at the track end [m]. |
| ✗ | `trk.end.y` | y at the track end [m]. |
| ✗ | `trk.end.z` | z at the track end [m], along the beam axis. |
| ✗ | `trk.end.t` | Time at the track end [s]. |
| ✗ | `trk.end.dcosu` | Direction cosine along u at the track end. |
| ✗ | `trk.end.dcosv` | Direction cosine along v at the track end. |
| ✗ | `trk.end.dcosx` | Direction cosine along x at the track end. |
| ✗ | `trk.end.dcosy` | Direction cosine along y at the track end. |
| ✗ | `trk.end.dcosz` | Direction cosine along z at the track end. |
| ✗ | `trk.end.eu` | Uncertainty on u at the track end. |
| ✗ | `trk.end.ev` | Uncertainty on v at the track end. |
| ✗ | `trk.end.ex` | Uncertainty on x at the track end. |
| ✗ | `trk.end.ey` | Uncertainty on y at the track end. |
| ✗ | `trk.end.edcosu` | Uncertainty on the u direction cosine at the track end. |
| ✗ | `trk.end.edcosv` | Uncertainty on the v direction cosine at the track end. |
| ✗ | `trk.end.edcosx` | Uncertainty on the x direction cosine at the track end. |
| ✗ | `trk.end.edcosy` | Uncertainty on the y direction cosine at the track end. |
| ✗ | `trk.end.edcosz` | Uncertainty on the z direction cosine at the track end. |
| ✗ | `trk.lin.plane` | Plane number at the track start from the linear fit. |
| ✗ | `trk.lin.u` | U coordinate at the track start from the linear fit [m]. |
| ✗ | `trk.lin.v` | V coordinate at the track start from the linear fit [m]. |
| ✗ | `trk.lin.x` | x at the track start from the linear fit [m]. |
| ✗ | `trk.lin.y` | y at the track start from the linear fit [m]. |
| ✗ | `trk.lin.z` | z at the track start from the linear fit [m], along the beam axis. |
| ✗ | `trk.lin.t` | Time at the track start from the linear fit [s]. |
| ✗ | `trk.lin.dcosu` | Direction cosine along u at the track start from the linear fit. |
| ✗ | `trk.lin.dcosv` | Direction cosine along v at the track start from the linear fit. |
| ✗ | `trk.lin.dcosx` | Direction cosine along x at the track start from the linear fit. |
| ✗ | `trk.lin.dcosy` | Direction cosine along y at the track start from the linear fit. |
| ✗ | `trk.lin.dcosz` | Direction cosine along z at the track start from the linear fit. |
| ✗ | `trk.lin.eu` | Uncertainty on u at the track start from the linear fit. |
| ✗ | `trk.lin.ev` | Uncertainty on v at the track start from the linear fit. |
| ✗ | `trk.lin.ex` | Uncertainty on x at the track start from the linear fit. |
| ✗ | `trk.lin.ey` | Uncertainty on y at the track start from the linear fit. |
| ✗ | `trk.lin.edcosu` | Uncertainty on the u direction cosine at the track start from the linear fit. |
| ✗ | `trk.lin.edcosv` | Uncertainty on the v direction cosine at the track start from the linear fit. |
| ✗ | `trk.lin.edcosx` | Uncertainty on the x direction cosine at the track start from the linear fit. |
| ✗ | `trk.lin.edcosy` | Uncertainty on the y direction cosine at the track start from the linear fit. |
| ✗ | `trk.lin.edcosz` | Uncertainty on the z direction cosine at the track start from the linear fit. |
| ✗ | `trk.fidvtx.dr` | Radial distance from the track start to the fiducial boundary [m]. |
| ✗ | `trk.fidvtx.dz` | Distance along z from the track start to the fiducial boundary [m]. |
| ✗ | `trk.fidvtx.trace` | Path length from the track start to where it leaves the fiducial volume [m]. |
| ✗ | `trk.fidvtx.tracez` | The same measured along z [m]. |
| ✗ | `trk.fidvtx.nplane` | Planes between the track start and the fiducial boundary. |
| ✗ | `trk.fidend.dr` | Radial distance from the track end to the fiducial boundary [m]. |
| ✗ | `trk.fidend.dz` | Distance along z from the track end to the fiducial boundary [m]. |
| ✗ | `trk.fidend.trace` | Path length from the track end to where it leaves the fiducial volume [m]. |
| ✗ | `trk.fidend.tracez` | The same measured along z [m]. |
| ✗ | `trk.fidend.nplane` | Planes between the track end and the fiducial boundary. |
| ✗ | `trk.fidall.dr` | Radial distance from the track as a whole to the fiducial boundary [m]. |
| ✗ | `trk.fidall.dz` | Distance along z from the track as a whole to the fiducial boundary [m]. |
| ✗ | `trk.fidall.trace` | Path length from the track as a whole to where it leaves the fiducial volume [m]. |
| ✗ | `trk.fidall.tracez` | The same measured along z [m]. |
| ✗ | `trk.fidall.nplane` | Planes between the track as a whole and the fiducial boundary. |
| ✗ | `trk.time.ndigit` | Digits used in the track's timing fit. |
| ✗ | `trk.time.chi2` | χ² of that fit. |
| ✗ | `trk.time.u0` | Fitted time at the track start, u view [s]. |
| ✗ | `trk.time.u1` | Fitted time at the track end, u view [s]. |
| ✗ | `trk.time.v0` | The same at the start, v view [s]. |
| ✗ | `trk.time.v1` | The same at the end, v view [s]. |
| ✗ | `trk.time.cdtds` | Fitted speed along the track, as a fraction of c. |
| ✗ | `trk.time.du` | Path length spanned in the u view [m]. |
| ✗ | `trk.time.dv` | The same in the v view [m]. |
| ✗ | `trk.time.dtds` | Fitted time gradient along the track [s/m]; its sign gives the direction of travel. |
| ✗ | `trk.time.t0` | Fitted time at the track start [s]. |
| ✗ | `trk.time.forwardRMS` | Timing residual RMS assuming the track ran forwards. |
| ✗ | `trk.time.forwardNDOF` | Degrees of freedom in that fit. |
| ✗ | `trk.time.backwardRMS` | The same assuming it ran backwards; comparing the two gives the direction. |
| ✗ | `trk.time.backwardNDOF` | Degrees of freedom in the backward fit. |
| ✗ | `trk.momentum.range` | Momentum from range, valid only for a stopping track [GeV/c]. |
| ✗ | `trk.momentum.qp` | Fitted charge over momentum [e/GeV]. |
| ✗ | `trk.momentum.eqp` | Uncertainty on it. |
| ✗ | `trk.momentum.best` | The better of the range and curvature estimates [GeV/c]. |
| ✗ | `trk.momentum.qp_rangebiased` | Charge over momentum from a fit seeded with the range estimate. |
| ✗ | `trk.momentum.eqp_rangebiased` | Uncertainty on that. |
| ✗ | `trk.fit.pass` | Whether the track fit converged. |
| ✗ | `trk.fit.ndof` | Degrees of freedom in the fit. |
| ✗ | `trk.fit.niterate` | Iterations it took. |
| ✗ | `trk.fit.nswimfail` | How many times swimming the track through the field failed. |
| ✗ | `trk.fit.chi2` | χ² of the fit. |
| ✗ | `trk.fit.cputime` | CPU seconds spent on it. |
| ✗ | `trk.fit.bave` | Mean magnetic field along the track [T]. |
| ✗ | `trk.cr.zenith` | Zenith angle of the track [rad]. |
| ✗ | `trk.cr.azimuth` | Azimuth of the track [rad]. |
| ✗ | `trk.cr.ra` | Right ascension of the arrival direction [rad]. |
| ✗ | `trk.cr.rahourangle` | The same as an hour angle. |
| ✗ | `trk.cr.dec` | Declination of the arrival direction [rad]. |
| ✗ | `trk.cr.juliandate` | Julian date of the snarl. |
| ✗ | `trk.cr.locsiderialtime` | Local sidereal time at the detector. |

## `evt` — 0 of 92 archived

Reconstructed events — tracks and showers assembled into a neutrino interaction candidate. Dropped in full, `evt.vtx.*` included: it is what MINOS concluded from the data, and an archive should hold what the detector recorded instead. Analyses do use the vertex for fiducial cuts, so this is the exclusion most likely to be questioned.

| | Branch | Meaning |
|--|--------|---------|
| ✗ | `evt.index` | Position of the event in its array. Row order already carries it. |
| ✗ | `evt.slc` | Index of the slice this event belongs to. |
| ✗ | `evt.ndigit` | Digits (single-end readouts) making up the event. |
| ✗ | `evt.nstpcnt` | Strips counted toward the event, including repeats across planes. |
| ✗ | `evt.nstrip` | Strips making up the event. |
| ✗ | `evt.stp` | Indices of the strips in this event, into the `stp` array. |
| ✗ | `evt.stpph0sigmap` | Attenuation-corrected pulse height at each strip, east end. |
| ✗ | `evt.stpph0mip` | The same in MIPs, east end. |
| ✗ | `evt.stpph0gev` | The same in GeV, east end. |
| ✗ | `evt.stpph1sigmap` | Attenuation-corrected pulse height at each strip, west end. |
| ✗ | `evt.stpph1mip` | The same in MIPs, west end. |
| ✗ | `evt.stpph1gev` | The same in GeV, west end. |
| ✗ | `evt.nshower` | Showers in the event. |
| ✗ | `evt.shw` | Indices of those showers, into the `shw` array. |
| ✗ | `evt.ntrack` | Tracks in the event. |
| ✗ | `evt.trk` | Indices of those tracks, into the `trk` array. |
| ✗ | `evt.primshw` | Index of the primary shower. |
| ✗ | `evt.primtrk` | Index of the primary track. |
| ✗ | `evt.contained` | Whether the event is fully contained in the detector. |
| ✗ | `evt.ph.raw` | Summed raw ADC over the event. |
| ✗ | `evt.ph.siglin` | The same, linearity-corrected. |
| ✗ | `evt.ph.sigcor` | The same, also corrected for fibre attenuation. |
| ✗ | `evt.ph.pe` | The same, in photoelectrons. |
| ✗ | `evt.ph.sigmap` | The same, normalised by the strip-to-strip response map. |
| ✗ | `evt.ph.mip` | The same, in units of a minimum-ionising particle. |
| ✗ | `evt.ph.gev` | The same, converted to GeV. |
| ✗ | `evt.plane.n` | Planes hit by the event. |
| ✗ | `evt.plane.nu` | Of those, planes in the u view. |
| ✗ | `evt.plane.nv` | Of those, planes in the v view. |
| ✗ | `evt.plane.beg` | First plane of the event. |
| ✗ | `evt.plane.begu` | First u-view plane of the event. |
| ✗ | `evt.plane.begv` | First v-view plane of the event. |
| ✗ | `evt.plane.end` | Last plane of the event. |
| ✗ | `evt.plane.endu` | Last u-view plane of the event. |
| ✗ | `evt.plane.endv` | Last v-view plane of the event. |
| ✗ | `evt.vtx.plane` | Plane number at the event vertex. |
| ✗ | `evt.vtx.u` | U coordinate at the event vertex [m]. |
| ✗ | `evt.vtx.v` | V coordinate at the event vertex [m]. |
| ✗ | `evt.vtx.x` | x at the event vertex [m]. |
| ✗ | `evt.vtx.y` | y at the event vertex [m]. |
| ✗ | `evt.vtx.z` | z at the event vertex [m], along the beam axis. |
| ✗ | `evt.vtx.t` | Time at the event vertex [s]. |
| ✗ | `evt.vtx.dcosu` | Direction cosine along u at the event vertex. |
| ✗ | `evt.vtx.dcosv` | Direction cosine along v at the event vertex. |
| ✗ | `evt.vtx.dcosx` | Direction cosine along x at the event vertex. |
| ✗ | `evt.vtx.dcosy` | Direction cosine along y at the event vertex. |
| ✗ | `evt.vtx.dcosz` | Direction cosine along z at the event vertex. |
| ✗ | `evt.vtx.eu` | Uncertainty on u at the event vertex. |
| ✗ | `evt.vtx.ev` | Uncertainty on v at the event vertex. |
| ✗ | `evt.vtx.ex` | Uncertainty on x at the event vertex. |
| ✗ | `evt.vtx.ey` | Uncertainty on y at the event vertex. |
| ✗ | `evt.vtx.edcosu` | Uncertainty on the u direction cosine at the event vertex. |
| ✗ | `evt.vtx.edcosv` | Uncertainty on the v direction cosine at the event vertex. |
| ✗ | `evt.vtx.edcosx` | Uncertainty on the x direction cosine at the event vertex. |
| ✗ | `evt.vtx.edcosy` | Uncertainty on the y direction cosine at the event vertex. |
| ✗ | `evt.vtx.edcosz` | Uncertainty on the z direction cosine at the event vertex. |
| ✗ | `evt.end.plane` | Plane number at the event end. |
| ✗ | `evt.end.u` | U coordinate at the event end [m]. |
| ✗ | `evt.end.v` | V coordinate at the event end [m]. |
| ✗ | `evt.end.x` | x at the event end [m]. |
| ✗ | `evt.end.y` | y at the event end [m]. |
| ✗ | `evt.end.z` | z at the event end [m], along the beam axis. |
| ✗ | `evt.end.t` | Time at the event end [s]. |
| ✗ | `evt.end.dcosu` | Direction cosine along u at the event end. |
| ✗ | `evt.end.dcosv` | Direction cosine along v at the event end. |
| ✗ | `evt.end.dcosx` | Direction cosine along x at the event end. |
| ✗ | `evt.end.dcosy` | Direction cosine along y at the event end. |
| ✗ | `evt.end.dcosz` | Direction cosine along z at the event end. |
| ✗ | `evt.end.eu` | Uncertainty on u at the event end. |
| ✗ | `evt.end.ev` | Uncertainty on v at the event end. |
| ✗ | `evt.end.ex` | Uncertainty on x at the event end. |
| ✗ | `evt.end.ey` | Uncertainty on y at the event end. |
| ✗ | `evt.end.edcosu` | Uncertainty on the u direction cosine at the event end. |
| ✗ | `evt.end.edcosv` | Uncertainty on the v direction cosine at the event end. |
| ✗ | `evt.end.edcosx` | Uncertainty on the x direction cosine at the event end. |
| ✗ | `evt.end.edcosy` | Uncertainty on the y direction cosine at the event end. |
| ✗ | `evt.end.edcosz` | Uncertainty on the z direction cosine at the event end. |
| ✗ | `evt.bleach.lateBucketPHFraction` | Fraction of the event's pulse height arriving in late time buckets — used to spot afterpulsing in the photomultipliers. |
| ✗ | `evt.bleach.timeWeightedPHFraction` | The same fraction weighted by arrival time. |
| ✗ | `evt.bleach.straightPHFraction` | Fraction of pulse height lying along a straight path through the event. |
| ✗ | `evt.bleach.fixedWindowPH` | Pulse height inside a fixed time window. |
| ✗ | `evt.bleach.eventDuration` | How long the event's hits span [s]. |
| ✗ | `evt.win.begplane` | First plane of the event's time window. |
| ✗ | `evt.win.endplane` | Last plane of it. |
| ✗ | `evt.win.begtime` | Start of the window [s]. |
| ✗ | `evt.win.endtime` | End of it [s]. |
| ✗ | `evt.win.totalQ` | Total charge in the window. |
| ✗ | `evt.win.specQ` | Charge in the window attributed to this event specifically. |
| ✗ | `evt.win.pinstQ` | Peak instantaneous charge in the window. |
| ✗ | `evt.win.utotalQ` | Total charge in the window, u view. |
| ✗ | `evt.win.uspecQ` | The event's own charge in the window, u view. |
| ✗ | `evt.win.upinstQ` | Peak instantaneous charge in the window, u view. |

## `mc` — 86 of 103 archived

Per-event interaction truth: what the generator says actually happened. Kinematics, channel and the truth 4-vectors. The bulk of the archive's simulation content, together with `stdhep`.

| | Branch | Meaning |
|--|--------|---------|
| ✗ | `mc.index` | Position of the interaction record in its array. Row order already carries it. |
| ✗ | `mc.stdhep[2]` | Index range pointing into `stdhep`; both are already joined per event. |
| ✗ | `mc.inu` | PDG code of the neutrino flavour. Duplicates the code on the `stdhep` initial-state neutrino row (`IstHEP == 0`). |
| ✓ | `mc.inunoosc` | PDG code of the neutrino flavour at production. Equals `inu` throughout the file checked, but would differ in a sample where flavours are swapped. |
| ✓ | `mc.itg` | PDG code of the struck target: `2212`/`2112` nucleons, a large nucleus code for coherent events, `11` for inverse muon decay. |
| ✗ | `mc.iboson` | Should carry the exchange boson's PDG code (Z⁰ = 23, W⁺ = 24) but holds a constant sentinel in every file checked. |
| ✓ | `mc.iresonance` | Channel: `1001` QE, `1002` resonance, `1003` DIS, `1004` coherent pion, `1005` inverse muon decay. |
| ✓ | `mc.iaction` | `0` = NC, `1` = CC. |
| ✓ | `mc.istruckq` | PDG id of the struck quark: `0` none (non-DIS), `1` d, `2` u. |
| ✓ | `mc.iflags` | Hadronisation model: `0` non-DIS, `1` old KNO, `2` modified KNO, `3` charm, `11`/`12`/`13` JETSET string/cluster/other. |
| ✓ | `mc.ndigu` | Raw digits in the u view truth-matched to this interaction. |
| ✓ | `mc.ndigv` | The same in the v view. A digit touching both views is counted in u only. |
| ✓ | `mc.tphu` | Summed pulse height over those u-view digits — **raw ADC, pedestal-subtracted**. |
| ✓ | `mc.tphv` | The same in the v view. |
| ✗ | `mc.a` | Mass number of the target nucleus. Encoded in the PDG code of the `stdhep` nucleus row; hydrogen when there is no such row. |
| ✗ | `mc.z` | Its atomic number, from the same code. |
| ✓ | `mc.sigma` | Cross section for this interaction. Units unconfirmed — passed through unchanged from NEUGEN. **???** |
| ✓ | `mc.sigmadiff` | Differential cross section. Same issue. **???** |
| ✓ | `mc.x` | Bjorken x. |
| ✓ | `mc.y` | Inelasticity y. The invariant form, not the lab-frame ratio, which ignores Fermi motion. |
| ✓ | `mc.q2` | Four-momentum transfer squared [GeV²]. |
| ✓ | `mc.w2` | Hadronic invariant mass squared [GeV²]. |
| ✓ | `mc.emfrac` | Electromagnetic fraction of the hadronic shower energy. Evaluated before final-state interactions. |
| ✗ | `mc.vtxx` | Interaction vertex x [m]. Duplicates `vtx[0]` on the `stdhep` neutrino row. |
| ✗ | `mc.vtxy` | Interaction vertex y [m]. Duplicates its `vtx[1]`. |
| ✗ | `mc.vtxz` | Interaction vertex z [m]. Duplicates its `vtx[2]`. |
| ✗ | `mc.p4neu[4]` | Neutrino 4-momentum. Duplicates `p4` on the `stdhep` neutrino row. |
| ✓ | `mc.p4neunoosc[4]` | Neutrino 4-momentum under the unoscillated hypothesis. |
| ✗ | `mc.p4tgt[4]` | Target 4-momentum. Duplicates `p4` on the `stdhep` struck-nucleon or nucleus row. |
| ✓ | `mc.p4shw[4]` | Final-state hadronic system. Not the sum of the `stdhep` hadrons: it is evaluated before final-state interactions — the rescattering of products inside the struck nucleus — whereas `stdhep` records the particles that emerge after them. |
| ✓ | `mc.p4mu1[4]` | Primary muon 4-momentum. Duplicates a `stdhep` lepton row, but with the energy component's sign flipped for the matter lepton. |
| ✗ | `mc.p4mu2[4]` | Second muon, where the event has one. Duplicates another `stdhep` muon row. |
| ✗ | `mc.p4el1[4]` | Electron 4-momentum. Duplicates an `stdhep` electron row. |
| ✗ | `mc.p4el2[4]` | Second electron. Duplicates another such row. |
| ✗ | `mc.p4tau[4]` | Tau 4-momentum. Duplicates an `stdhep` tau row, and never non-zero in any file checked — no ν_τ events. |
| ✗ | `mc.flux.index` | Position of the flux record in its array. |
| ✓ | `mc.flux.fluxrun` | gnumi beam-simulation run number. |
| ✓ | `mc.flux.fluxevtno` | Event number within that run. |
| ✓ | `mc.flux.ndxdz` | Neutrino dx/dz slope at the decay point. |
| ✓ | `mc.flux.ndydz` | Neutrino dy/dz slope at the decay point. |
| ✓ | `mc.flux.npz` | Neutrino momentum along z at the decay point [GeV/c]. |
| ✓ | `mc.flux.nenergy` | Neutrino energy at the decay point [GeV]. |
| ✓ | `mc.flux.ndxdznear` | Neutrino dx/dz slope toward the Near Detector centre. |
| ✓ | `mc.flux.ndydznear` | Neutrino dy/dz slope toward the Near Detector centre. |
| ✓ | `mc.flux.nenergynear` | Energy it would have at the Near Detector centre [GeV]. |
| ✓ | `mc.flux.nwtnear` | Weight turning generated events into a flux prediction at the Near Detector. |
| ✓ | `mc.flux.ndxdzfar` | Neutrino dx/dz slope toward the Far Detector centre. |
| ✓ | `mc.flux.ndydzfar` | Neutrino dy/dz slope toward the Far Detector centre. |
| ✓ | `mc.flux.nenergyfar` | Energy it would have at the Far Detector centre [GeV]. |
| ✓ | `mc.flux.nwtfar` | The same weight for the Far Detector. With the near fields, this pair is what the near/far extrapolation is built from. |
| ✓ | `mc.flux.norig` | Marked *(ignore)* in the gnumi source. |
| ✓ | `mc.flux.ndecay` | Tag identifying the decay mode that produced the neutrino. |
| ✓ | `mc.flux.ntype` | Neutrino type, as a PDG code. |
| ✓ | `mc.flux.vx` | x of the decay vertex [cm]. |
| ✓ | `mc.flux.vy` | y of the decay vertex [cm]. |
| ✓ | `mc.flux.vz` | z of the decay vertex [cm]. |
| ✓ | `mc.flux.pdpx` | Parent px at the decay point [GeV/c]. |
| ✓ | `mc.flux.pdpy` | Parent py at the decay point [GeV/c]. |
| ✓ | `mc.flux.pdpz` | Parent pz at the decay point [GeV/c]. |
| ✓ | `mc.flux.ppdxdz` | Parent dx/dz slope at the decay point. |
| ✓ | `mc.flux.ppdydz` | Parent dy/dz slope at the decay point. |
| ✓ | `mc.flux.pppz` | Parent pz at the decay point [GeV/c]. |
| ✓ | `mc.flux.ppenergy` | Parent energy at the decay point [GeV]. |
| ✓ | `mc.flux.ppmedium` | GEANT medium the parent was produced in. |
| ✓ | `mc.flux.ptype` | Parent particle type, as a PDG code. |
| ✓ | `mc.flux.ppvx` | x of the parent's production vertex [cm]. |
| ✓ | `mc.flux.ppvy` | y of the parent's production vertex [cm]. |
| ✓ | `mc.flux.ppvz` | z of the parent's production vertex [cm]. |
| ✓ | `mc.flux.muparpx` | Where the parent was a muon, its own parent's px [GeV/c]. |
| ✓ | `mc.flux.muparpy` | The same, py. |
| ✓ | `mc.flux.muparpz` | The same, pz. |
| ✓ | `mc.flux.mupare` | The same, energy [GeV]. |
| ✓ | `mc.flux.necm` | Neutrino energy in the parent's centre-of-mass frame [GeV]. |
| ✓ | `mc.flux.nimpwt` | Importance weight from the simulation. |
| ✓ | `mc.flux.xpoint` | Marked *(unused)* in the gnumi source. |
| ✓ | `mc.flux.ypoint` | Marked *(unused)* in the gnumi source. |
| ✓ | `mc.flux.zpoint` | Marked *(unused)* in the gnumi source. |
| ✓ | `mc.flux.tvx` | x where the parent left the target [cm]. |
| ✓ | `mc.flux.tvy` | y where the parent left the target [cm]. |
| ✓ | `mc.flux.tvz` | z where the parent left the target [cm]. |
| ✓ | `mc.flux.tpx` | Parent px at target exit [GeV/c]. |
| ✓ | `mc.flux.tpy` | Parent py at target exit [GeV/c]. |
| ✓ | `mc.flux.tpz` | Parent pz at target exit [GeV/c]. |
| ✓ | `mc.flux.tptype` | Type of that parent, as a PDG code. |
| ✓ | `mc.flux.tgen` | Which generation of the hadronic cascade the parent belongs to. This is what hadron-production reweighting needs. |
| ✓ | `mc.flux.tgptype` | Type of the particle that came off the target, as a PDG code. |
| ✓ | `mc.flux.tgppx` | Its px at the interaction [GeV/c]. |
| ✓ | `mc.flux.tgppy` | Its py [GeV/c]. |
| ✓ | `mc.flux.tgppz` | Its pz [GeV/c]. |
| ✓ | `mc.flux.tprivx` | x of the primary proton's interaction vertex [cm]. |
| ✓ | `mc.flux.tprivy` | y of it [cm]. |
| ✓ | `mc.flux.tprivz` | z of it [cm]. |
| ✓ | `mc.flux.beamx` | x where the primary proton entered [cm]. |
| ✓ | `mc.flux.beamy` | y where it entered [cm]. |
| ✓ | `mc.flux.beamz` | z where it entered [cm]. |
| ✓ | `mc.flux.beampx` | Primary proton px [GeV/c]. |
| ✓ | `mc.flux.beampy` | Primary proton py [GeV/c]. |
| ✓ | `mc.flux.beampz` | Primary proton pz [GeV/c]. |
| ✗ | `mc.fluxwgt.index` | Position of the flux-weight record in its array. |
| ✓ | `mc.fluxwgt.beam[33]` | Name of the beam configuration the weight applies to, as 33 characters. |
| ✓ | `mc.fluxwgt.version` | Version of the reweighting that produced it. |
| ✓ | `mc.fluxwgt.weight` | Flux weight for this event. |
| ✓ | `mc.fluxwgt.weighterr` | Uncertainty on that weight. |

## `stdhep` — 8 of 11 archived

The truth particle stack, in HEPEVT convention: one row per particle, variable length per event. The incoming neutrino, the struck target, and everything in the final state. Every event carries one `IstHEP == 999`, `IdHEP == 0` row — a *rootino*, a null placeholder never tracked.

| | Branch | Meaning |
|--|--------|---------|
| ✗ | `stdhep.index` | Position of the particle in its array. Row order already carries it. |
| ✗ | `stdhep.mc` | Index of the interaction this particle belongs to; the reverse of `mc.stdhep[2]`, and both are already joined per event. |
| ✓ | `stdhep.parent[2]` | Indices of the particle's parents. |
| ✓ | `stdhep.child[2]` | Indices of its daughters. |
| ✓ | `stdhep.IstHEP` | HEPEVT status code: `0` initial state, `1` final state, `11` struck nucleon. |
| ✓ | `stdhep.IdHEP` | PDG code. |
| ✓ | `stdhep.mass` | Rest mass [GeV]. |
| ✓ | `stdhep.p4[4]` | 4-momentum: `(px, py, pz)` then energy, all GeV. |
| ✓ | `stdhep.vtx[4]` | Production 4-position: `(x, y, z)` in metres, then time in seconds. |
| ✓ | `stdhep.ndethit` | How many digits the particle deposited energy in. |
| ✗ | `stdhep.dethit[2]` | The particle's first and last hit — plane, strip, position and momentum for each. Left out because uproot reads it as a C++ struct array, which would need unpacking work. **This is the one exclusion that loses information with no way to recover it**, and is worth revisiting. |

## `digihit` — 0 of 18 archived

The finest-grained truth there is: per particle per strip, where it entered and left and how much it deposited. **Empty in every file checked**, so there is nothing to archive. `check_exclusions.py` refuses any file where that stops being true.

| | Branch | Meaning |
|--|--------|---------|
| ✗ | `digihit.index` | Position of the record in its array. |
| ✗ | `digihit.planeview` | View of the strip: `2` = U, `3` = V. |
| ✗ | `digihit.strip` | Strip number within the plane. |
| ✗ | `digihit.plane` | Plane number. |
| ✗ | `digihit.trkId` | Simulation track id of the particle that deposited the energy. |
| ✗ | `digihit.pId` | PDG code of that particle. |
| ✗ | `digihit.t0` | Time entering the scintillator [s]. |
| ✗ | `digihit.x0` | x entering [m]. |
| ✗ | `digihit.y0` | y entering [m]. |
| ✗ | `digihit.z0` | z entering [m]. |
| ✗ | `digihit.t1` | Time leaving [s]. |
| ✗ | `digihit.x1` | x leaving [m]. |
| ✗ | `digihit.y1` | y leaving [m]. |
| ✗ | `digihit.z1` | z leaving [m]. |
| ✗ | `digihit.dS` | Path length through the scintillator [m]. |
| ✗ | `digihit.dE` | Energy deposited [GeV]. |
| ✗ | `digihit.pE` | Photoelectrons the deposit produced. |
| ✗ | `digihit.failbits` | Flags recording problems with the hit. |

## `thstp` — 5 of 6 archived

Truth for each strip in `stp`: which simulated particles deposited energy there, and in what proportion. Exactly one record per hit, so it lines up row-for-row with the hit variables. Archived, because it labels data that is itself archived.

| | Branch | Meaning |
|--|--------|---------|
| ✗ | `thstp.index` | Position of the record in its array. Row order already carries it. |
| ✓ | `thstp.neumc` | Index of the interaction (`mc` record) responsible for the strip. |
| ✓ | `thstp.nneu` | How many interactions contributed to it. |
| ✓ | `thstp.sigflg` | Signal flag: whether the hit is signal or noise. |
| ✓ | `thstp.stdhep[3]` | Up to three contributing `stdhep` particle indices. |
| ✓ | `thstp.phfrac[3]` | Fraction of the strip's pulse height from each of those. |

## `thslc` — 0 of 7 archived

Truth matching for reconstructed slices. Unlike `thstp`, which labels the strips the archive keeps, this is meaningless without the reconstructed object it describes.

| | Branch | Meaning |
|--|--------|---------|
| ✗ | `thslc.index` | Position of the record in its array. |
| ✗ | `thslc.neumc` | Index of the interaction the slice was matched to. |
| ✗ | `thslc.neustdhep` | Index of the truth particle it was matched to. |
| ✗ | `thslc.nneu` | How many interactions contributed to the slice. |
| ✗ | `thslc.purity` | Fraction of the slice's energy that came from that interaction. |
| ✗ | `thslc.secondpurity` | Purity with respect to the second-best matching interaction. |
| ✗ | `thslc.complete` | Fraction of the matched interaction's energy that ended up in the slice. |

## `thshw` — 0 of 8 archived

Truth matching for reconstructed showers. Unlike `thstp`, which labels the strips the archive keeps, this is meaningless without the reconstructed object it describes.

| | Branch | Meaning |
|--|--------|---------|
| ✗ | `thshw.index` | Position of the record in its array. |
| ✗ | `thshw.neumc` | Index of the interaction the shower was matched to. |
| ✗ | `thshw.neustdhep` | Index of the truth particle it was matched to. |
| ✗ | `thshw.purity` | Fraction of the shower's energy that came from that interaction. |
| ✗ | `thshw.completeall` | Fraction of the interaction's energy the shower captured, over the whole snarl. |
| ✗ | `thshw.completeslc` | The same restricted to the slice. |
| ✗ | `thshw.completeallnopecut` | The same as `completeall`, without the photoelectron threshold. |
| ✗ | `thshw.completeslcnopecut` | The same as `completeslc`, without it. |

## `thtrk` — 0 of 7 archived

Truth matching for reconstructed tracks. Unlike `thstp`, which labels the strips the archive keeps, this is meaningless without the reconstructed object it describes.

| | Branch | Meaning |
|--|--------|---------|
| ✗ | `thtrk.index` | Position of the record in its array. |
| ✗ | `thtrk.neumc` | Index of the interaction the track was matched to. |
| ✗ | `thtrk.neustdhep` | Index of the truth particle it was matched to. |
| ✗ | `thtrk.trkstdhep` | Index of the truth particle the track itself was matched to. |
| ✗ | `thtrk.purity` | Fraction of the track's energy that came from that interaction. |
| ✗ | `thtrk.completeall` | Fraction of the interaction's energy the track captured, over the whole snarl. |
| ✗ | `thtrk.completeslc` | The same restricted to the slice. |

## `thevt` — 0 of 8 archived

Truth matching for reconstructed reconstructed events. Unlike `thstp`, which labels the strips the archive keeps, this is meaningless without the reconstructed object it describes.

| | Branch | Meaning |
|--|--------|---------|
| ✗ | `thevt.index` | Position of the record in its array. |
| ✗ | `thevt.neumc` | Index of the interaction the reconstructed event was matched to. |
| ✗ | `thevt.neustdhep` | Index of the truth particle it was matched to. |
| ✗ | `thevt.purity` | Fraction of the reconstructed event's energy that came from that interaction. |
| ✗ | `thevt.completeall` | Fraction of the interaction's energy the reconstructed event captured, over the whole snarl. |
| ✗ | `thevt.completeslc` | The same restricted to the slice. |
| ✗ | `thevt.completeallnopecut` | The same as `completeall`, without the photoelectron threshold. |
| ✗ | `thevt.completeslcnopecut` | The same as `completeslc`, without it. |

